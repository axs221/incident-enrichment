import React from "react";
import ReactDOM from "react-dom";
import "./index.css";
import Routes from "./Routes";
// import registerServiceWorker from "./registerServiceWorker"; // TODO: remove if not needed

ReactDOM.render(<Routes />, document.getElementById("root"));
// registerServiceWorker(); // TODO: remove if not needed
